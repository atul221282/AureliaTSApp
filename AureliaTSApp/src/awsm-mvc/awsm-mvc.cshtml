<template>
    <section class="au-animate">
        @DateTime.Now
        <br />
        ${user.UserName}

        <div repeat.for="li of list">
            ${li.id}
        </div>
    </section>
</template>